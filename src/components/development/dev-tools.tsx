"use client";

import { useOnboarding } from "@/hooks/use-onboarding";
import { Button } from "@/components/ui/button";

export const DevTools = () => {
    // This button is for development purposes and has been removed as per the user's request.
    return null;
};
